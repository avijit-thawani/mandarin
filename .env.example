# Supabase Configuration (Required for cloud sync)
# Get these from your Supabase project: Settings > API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Development Auto-Login (Optional)
# When both are set, the app will auto-login in dev mode
VITE_DEV_USER_EMAIL=
VITE_DEV_USER_PASSWORD=

# Web Push (Optional - required for PWA reminders)
# Use "npx web-push generate-vapid-keys" and paste public key here
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key-here

# Anthropic API Key (Optional - only for content processing scripts)
# Used by content/hsk1/*.py scripts for OCR correction
ANTHROPIC_API_KEY=
